/** @jsx jsx */
import { jsx } from "@emotion/core";
import * as React from "react";

interface LoadingPlaceholderProps {}

export function LoadingPlaceholder({  }: LoadingPlaceholderProps) {
  return <div />;
}
