import { Button, Divider, Container } from "../../../../src"
import "./button.css"
import { Article } from "../../components/Layout"
import { Props } from "../../components/Props"
import { IconList } from "../../components/IconList"
import SEO from "../../components/SEO"
import { Nav } from "../../components/Nav"

<Nav title="Icon" />
<SEO title="Button" />

<Container>
<Article sidebar={[
  { id: 'Icon', name: 'Icon' },
  { id: 'Basic usage', name: 'Basic usage' },
  { id: 'Available icons', name: 'Available icons'},
  { id: 'Props', name: 'Props'}
]}>

# Icon

We make use of Blueprint's wonderful collection of icons. For accessibility purposes, icons should be used in combination with descriptive text which is either visible or hidden using the `VisuallyHidden` component.

#### Basic usage

```jsx
<Icon icon="menu" />
```

#### Available icons

<IconList />

<Props names={["Icon"]} />

</Article>
</Container>
