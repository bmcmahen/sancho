import { Badge, Divider, Container } from "../../../../src"
import "./button.css"
import { Article } from "../../components/Layout"
import { Props } from "../../components/Props"
import SEO from "../../components/SEO"

<SEO title="Badge" />

<Container>
<Article sidebar={[
  { id: 'Badge', name: 'Badge' },
  { id: 'Basic usage', name: 'Basic usage' },
  { id: 'Props', name: 'Props' }
]}>

# Badge

Use a badge to indicate a count in a menu, such as unread messages.

#### Basic usage

```jsx
<Badge>23</Badge>
```

<Props names={["Badge"]} />

</Article>
</Container>
