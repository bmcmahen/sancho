import { Container } from "../../../../src"
import { Article } from "../../components/Layout"
import { Props } from "../../components/Props"
import SEO from "../../components/SEO"

<SEO title="SkipNav" />

<Container>
<Article sidebar={[
  { id: 'SkipNav', name: 'SkipNav' },
  { id: 'Basic usage', name: 'Basic usage' },
  { id: 'Props', name: 'Props'}
]}>

# SkipNav

The [SkipNav](https://ui.reach.tech/skip-nav) component from Reach-UI. This allows a user using a screen reader to skip over the navigation to quickly access the main content.

SkipNav is implemented using two components:

- **SkipNavLink**: The link to be placed at the top of your app tree. It will remain hidden unless focused via keyboard.
- **SkipNavContent**: Place this component before your content and after your navigation.

See the [documentation](https://ui.reach.tech/skip-nav) at the Reach-UI website for full usage details, and thank Ryan Florence for making accessibility a priority.

#### Basic usage

```.jsx
<React.Fragment>
  {/* the SkipNavLink goes at the top of your app */}
  <SkipNavLink />
  <div>
    <div>Your nav contents</div>
    {/* while SkipNavContent goes just before your main content */}
    <SkipNavContent />
    <main>The main content</main>
  </div>
</React.Fragment>
```

<Props names={["SkipNav"]} />

</Article>
</Container>
