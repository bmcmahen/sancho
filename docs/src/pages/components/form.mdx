import { Button, Divider, Container } from "../../../../src"
import "./button.css"
import { Article } from "../../components/Layout"
import { Props } from "../../components/Props"
import SEO from "../../components/SEO"

<SEO title="Button" />

<Container>
<Article sidebar={[
  { id: 'Forms', name: 'Forms' },
  { id: 'InputGroup usage', name: 'InputGroup usage' },
  { id: 'Input usage', name: 'Input usage' },
  { id: 'Textarea usage', name: 'Textarea usage'},
  { id: 'Custom', name: 'Custom example' },
  { id: 'Props', name: 'Props'}
]}>

# Forms

The forms module exposes four different components: `InputGroup`, `Input`, `TextArea`, `Label`.

#### InputGroup usage

`InputGroup` composes the other three and is a useful component for building your typical forms. If you need more customization, you can use the other three.

```jsx
<form>
  <InputGroup error="Required field" label="Email address">
    <Input type="email" placeholder="name@example.com" />
  </InputGroup>
  <InputGroup
    label="Example textarea"
    helpText="Please provide a brief description of yourself. This will go on your profile."
  >
    <TextArea />
  </InputGroup>

  <InputGroup label="Gender">
    <Select>
      <option>Female</option>
      <option>Male</option>
      <option>Other</option>
    </Select>
  </InputGroup>

  <InputGroup label="Do you accept our terms?">
    <Check label="Sure, whatevs" />
  </InputGroup>
</form>
```

#### Input usage

```jsx
<form>
  <Input defaultValue="Just a standard input" />
</form>
```

#### TextArea usage

```jsx
<form>
  <TextArea defaultValue="Just a standard input" />
</form>
```

#### Custom

```jsx
<form>
  <Label htmlFor="example">My example</Label>
  <TextArea id="example" defaultValue="Just a textarea with a label" />
</form>
```

<Props names={["InputGroup"]} />

</Article>
</Container>
